version: '3'

services:
  postgres-test:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: test-post
    ports:
      - "5432:5432"
    networks:
      - mynetwork
networks:
  mynetwork: